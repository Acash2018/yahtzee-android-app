<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked = will re-roll (bright) -->
    <item android:state_checked="true" android:drawable="@drawable/die5"/>
    <!-- Unchecked = held (dim) -->
    <item android:drawable="@drawable/die5_dim"/>
</selector>
